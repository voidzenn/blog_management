<div class="show-content flex flex-wrap mx-20 my-10">
  <div class="mb-10">
    <%= link_to "Back to Home", root_path, class: "return-action text-blue-600 text-2xl font-bold no-underline hover:text-blue-500" %>
  </div>
  <div class="w-full">
    <p class="text-2xl font-semibold"><%= sanitize_data(@post.title) %></p>
  </div>
  <div class="post-content w-full mx-auto mt-5">
    <p><%= sanitize_data(@post.content) %></p>
  </div>
</div>
